export enum DrawerMode {
    edit = 'edit',
    create = 'create',
    invite = 'invite'
}

export enum InviteStatus {
    accepted = 'accepted',
    pending = 'pending',
    rejected = 'rejected',
}

export const dateFullFormat = 'DD.MM.YYYY'
export const dateShortFormat = 'DD.MM'